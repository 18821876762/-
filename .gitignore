# Python virtual environment
.venv/
venv/
env/

# Python cache
__pycache__/
*.pyc
*.pyo

# Build / dist
build/
dist/
*.egg-info/

# IDE / OS
.idea/
.vscode/
.vs/
.DS_Store
*.log

# CodeBuddy project data
.codebuddy/

# Secrets / credentials — 切勿提交（含真实学习通会话，误提交即永久泄露）
cookies.json
perception/cx_crawler/cookies.json

# Crawler output（含个人课程/进度数据，属轻度 PII）
perception/cx_crawler/output/

# 绿色版 Node（本地语法校验用，非运行时依赖，随环境安装）
node_home/
node-v*-win-x64.zip

# 依赖安装产物（_sim 等模拟用例的 npm 依赖，非源码，勿入版本库）
node_modules/

# 可重新生成的临时产物（调查 diff 转储、jsdom 回归比对输出，勿入版本库）
.tmp_*.diff
ecosystem/_sim/out-*-NEW.txt

# git bundle 交付物（便携仓库副本，用于离线搬运，勿入版本库）
*.bundle
